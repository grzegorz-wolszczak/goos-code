plugins {
    id 'java'
}

version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    dependencies {
        compile fileTree(dir: 'lib/develop', include: ['*.jar'])
        compile fileTree(dir: 'lib/deploy', include: ['*.jar'])
    }
}
